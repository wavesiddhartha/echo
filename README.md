# echo

A distraction-free, minimalist relationship check-in manager built with React Native (Expo) and Next.js.

## Getting Started

Run the React Native Expo development app:

```bash
npm start
```
